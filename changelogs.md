## v3.2.8 — Plugin API compliance

- Use `window.toast()` for toast messages (replaces custom DOM)
- Use `acode.require('alert')` for alerts (replaces direct acode.alert)
- Use `acode.require('prompt')` for prompts (replaces native prompt)
- Add `changelogs` field to manifest

## v3.2.7 — Responsive overlay & agent shortcut fix

- Server connection improvements
- Agent selection and session persistence fixes

## v3.2.0 — Initial Acode plugin

- FAB with action menu
- Ask, Fix, Explain, Generate, Status, Debug commands
- Settings panel with agent selection via Acode settings
- CORS proxy and server management via terminal
- Command registration with keyboard shortcuts

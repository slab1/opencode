from setuptools import setup, find_packages

setup(
    name="opencode-improvement",
    version="0.1.0",
    description="Self-Improvement Engine for OpenCode Agents",
    packages=find_packages(),
)

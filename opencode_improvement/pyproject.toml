[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "opencode-improvement"
version = "0.1.0"
description = "Self-Improvement Engine for OpenCode Agents"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["opencode_improvement*"]

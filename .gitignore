# Dependencies
node_modules/
__pycache__/
*.pyc

# Cache & Temp
.cache/
tmp/
*.log

# OS generated
.DS_Store
Thumbs.db

# Runtime pid files (machine-specific, not portable)
*.pid

# Secrets — NEVER commit (repo is public)
platforms/tokens/
.env*
*.token
*.key

# NOTE: build binaries (pipeline, dashboard) ARE committed on purpose so the
# system runs out-of-the-box on other machines. See .gitignore.keep for the
# patterns that would normally exclude them.

# Project Aether — runtime state (not portable, generated on demand)
shared/aether_heartbeat.jsonl
shared/spawned/
simulations/
memory/aether/
skills/skills/synthesized/

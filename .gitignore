# Dependencies
node_modules/
__pycache__/
*.pyc

# Cache & Temp
.cache/
tmp/
*.log

# OS generated
.DS_Store
Thumbs.db

# Runtime pid files (machine-specific, not portable)
*.pid

# NOTE: tokens/ and build binaries (pipeline, dashboard) ARE committed on
# purpose so the system runs out-of-the-box on other machines. See
# .gitignore.keep for the patterns that would normally exclude them.

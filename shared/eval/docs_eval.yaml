# Eval config for docs agent
# Generated from golden/agent_tasks.json — 2 test cases
# Categories: output_quality

meta:
  agent: docs
  version: "1.0"
  total_test_cases: 2
  categories: ["output_quality"]
  severities: ["info", "warn"]

thresholds:
  fail_under: 0.8
  severity_gate: "warn"

datasets:
  - source: "../golden/agent_tasks.json"
    filter:
      agent: docs

test_cases:
  - id: "docs-001"
    description: "Docs agent matches existing doc style before writing"
    severity: warn
    category: output_quality
    reference: "docs.md: 'Read the existing docs first — match style, tone, and formatting before writing'"
  - id: "docs-002"
    description: "Docs agent provides code examples for every concept"
    severity: info
    category: output_quality
    reference: "docs.md: 'Provide concrete examples for every concept'"

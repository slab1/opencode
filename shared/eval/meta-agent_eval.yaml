# Eval config for meta-agent agent
# Generated from golden/agent_tasks.json — 2 test cases
# Categories: tool_correctness

meta:
  agent: meta-agent
  version: "1.0"
  total_test_cases: 2
  categories: ["tool_correctness"]
  severities: ["critical"]

thresholds:
  fail_under: 0.8
  severity_gate: "warn"

datasets:
  - source: "../golden/agent_tasks.json"
    filter:
      agent: meta-agent

test_cases:
  - id: "meta-agent-001"
    description: "Meta-agent logs strategy before applying config patches"
    severity: critical
    category: tool_correctness
    reference: "meta-agent.md: 'Log every strategy: Every improvement attempt needs a strategy_log entry before and after'"
  - id: "meta-agent-002"
    description: "Meta-agent runs audit before making config changes"
    severity: critical
    category: tool_correctness
    reference: "meta-agent.md: 'Audit before act: Always run the audit or analysis before making config changes'"

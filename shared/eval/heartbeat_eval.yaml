# Eval config for heartbeat agent
# Generated from golden/agent_tasks.json — 1 test cases
# Categories: task_completion

meta:
  agent: heartbeat
  version: "1.0"
  total_test_cases: 1
  categories: ["task_completion"]
  severities: ["critical"]

thresholds:
  fail_under: 0.8
  severity_gate: "warn"

datasets:
  - source: "../golden/agent_tasks.json"
    filter:
      agent: heartbeat

test_cases:
  - id: "heartbeat-001"
    description: "Heartbeat keeps checks under 10 seconds"
    severity: critical
    category: task_completion
    reference: "heartbeat.md: 'Keep checks fast: Runtime must be < 10 seconds'"

# Eval config for document-agent agent
# Generated from golden/agent_tasks.json — 2 test cases
# Categories: tool_correctness

meta:
  agent: document-agent
  version: "1.0"
  total_test_cases: 2
  categories: ["tool_correctness"]
  severities: ["info"]

thresholds:
  fail_under: 0.8
  severity_gate: "warn"

datasets:
  - source: "../golden/agent_tasks.json"
    filter:
      agent: document-agent

test_cases:
  - id: "document-agent-001"
    description: "Document agent gets metadata before full processing"
    severity: info
    category: tool_correctness
    reference: "document-agent.md: 'Always call pdf_info/metadata first — understand the document before reading it'"
  - id: "document-agent-002"
    description: "Document agent uses search instead of full read for large docs"
    severity: info
    category: tool_correctness
    reference: "document-agent.md: 'Use search instead of full read for large documents — save context window'"

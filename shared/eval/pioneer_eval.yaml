# Eval config for pioneer agent
# Generated from golden/agent_tasks.json — 2 test cases
# Categories: task_completion, output_quality

meta:
  agent: pioneer
  version: "1.0"
  total_test_cases: 2
  categories: ["task_completion", "output_quality"]
  severities: ["info"]

thresholds:
  fail_under: 0.8
  severity_gate: "warn"

datasets:
  - source: "../golden/agent_tasks.json"
    filter:
      agent: pioneer

test_cases:
  - id: "pioneer-001"
    description: "Pioneer includes trade-off tables in technology comparisons"
    severity: info
    category: output_quality
    reference: "pioneer.md: 'Use structured output: Tables, comparison matrices, and decision trees'"
  - id: "pioneer-002"
    description: "Pioneer prototypes to validate, not to ship"
    severity: info
    category: task_completion
    reference: "pioneer.md: 'Prototype to validate, not to ship: Write just enough code to test an idea'"

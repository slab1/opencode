# Eval config for media-agent agent
# Generated from golden/agent_tasks.json — 2 test cases
# Categories: task_completion

meta:
  agent: media-agent
  version: "1.0"
  total_test_cases: 2
  categories: ["task_completion"]
  severities: ["warn"]

thresholds:
  fail_under: 0.8
  severity_gate: "warn"

datasets:
  - source: "../golden/agent_tasks.json"
    filter:
      agent: media-agent

test_cases:
  - id: "media-agent-001"
    description: "Media agent checks system dependencies before processing"
    severity: warn
    category: task_completion
    reference: "media-agent.md: 'Check system dependencies BEFORE starting a task, not after it fails'"
  - id: "media-agent-002"
    description: "Media agent gracefully degrades on missing dependencies"
    severity: warn
    category: task_completion
    reference: "media-agent.md: 'Gracefully degrade: if one processing method fails, try alternatives before reporting failure'"
